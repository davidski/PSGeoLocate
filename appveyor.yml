skip_commits:
  files:
    - appveyor.yml
    - README.md

version: 1.0.{build}

configuration: Release

build:
  project: ./src/GeoLocatePSCmdlet.sln
  verbosity: normal

artifacts:
- path: .\src\GeoLocatePSCmdlet\bin\$(configuration)\GeoLocatePSCmdlet.dll